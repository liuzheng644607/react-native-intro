
/**
groupMap = {
  groupA: {
    1: {
      content: React.Element,
      disable: boolean,
      target: IntroComponent,
      refTarget: ref,
      _index: number,
    }
  }
}
 */
export const groupMap = {};

export const DEFAULT_GROUP = "DEFAULT_GROUP";

export const DEFAULTOPTIONS = {
  group: DEFAULT_GROUP
};